{
	"name": "jwt-secret-cli",
	"version": "1.0.1",
	"description": "A CLI tool to generate secure JWT secrets",
	"main": "lib/generateSecret.js",
	"bin": {
		"jwt-secret-cli": "./bin/index.js"
	},
	"scripts": {
		"test": "jest",
		"lint": "eslint . --config eslint.config.js",
		"prepublishOnly": "npm test && npm run lint"
	},
	"keywords": [
		"jwt",
		"secret",
		"cli",
		"token",
		"generator",
		"security"
	],
	"author": "LayerDynamics",
	"license": "Unlicense",
	"dependencies": {
		"commander": "^10.0.0"
	},
	"devDependencies": {
		"jest": "^29.0.0",
		"eslint": "^9.6.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/LayerDynamics/jwt-secret-cli.git"
	},
	"engines": {
		"node": ">=14.0.0"
	}
}
